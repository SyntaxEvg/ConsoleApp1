// Блокировать все внешние запросы по умолчанию
define('WP_HTTP_BLOCK_EXTERNAL', true);

// Белый список разрешённых доменов (управляемый список)
// Закомментируй строку с доменом, чтобы заблокировать его
define('WP_ACCESSIBLE_HOSTS', implode(',', array(
    // 'api.wordpress.org',           // Закомментировано = заблокировано
    // 'wordpress.org',                // Закомментировано = заблокировано  
    // 'downloads.wordpress.org',      // Закомментировано = заблокировано
    // 's.w.org',                      // Эмодзи и прочее
    // 'ps.w.org',                     // Photon (оптимизация изображений)
    
    // Раскомментируй, если нужны эти сервисы:
    // 'cdn.example.com',              // Твой CDN
    // 'fonts.googleapis.com',         // Google Fonts
    // 'maps.googleapis.com',          // Google Maps
)));

// Отключить автообновления
define('AUTOMATIC_UPDATER_DISABLED', true);
define('WP_AUTO_UPDATE_CORE', false);
