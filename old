Понял! Вижу календарь с диапазоном дат. Попробуйте так:
Вариант 1 - Через контейнер календаря:
javascript// Найти элемент календаря
let calendar = document.querySelector('.vc-pane-container')
console.log(calendar)

// Или через родительский контейнер
let dateRange = document.querySelector('.flex.items-center.gap-2')
console.log(dateRange)
Вариант 2 - Это похоже на Vue Calendar (v-calendar):
javascript// Найти Vue компонент
let calendarElement = document.querySelector('[data-v-c42d2aba]')
let vueInstance = calendarElement.__vue__ || calendarElement.__vueParentComponent

// Посмотреть данные
console.log(vueInstance)
Вариант 3 - Через span с датами:
javascript// Найти span с начальной датой
let startDate = document.querySelector('.text-gray-700.font-medium')
console.log(startDate.textContent) // "20 ноября"

// Изменить текст (визуально)
startDate.textContent = "25 ноября"
Вариант 4 - Найти все элементы с data-v атрибутами:
javascript// Посмотреть все Vue компоненты
document.querySelectorAll('[data-v-c42d2aba]').forEach(el => {
    console.log(el)
    console.log(el.__vue__)
})
Вариант 5 - Через корневой Vue инстанс:
javascript// Если есть глобальный Vue
window.$vm0 // в Vue DevTools
// или
document.querySelector('#app').__vue__
