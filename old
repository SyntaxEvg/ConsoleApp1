# Проверить все PHP файлы рекурсивно
find /var/www -name "*.php" -exec php -l {} \; 2>&1 | grep -v "No syntax errors"
Errors parsing /var/www/wp-content/_plugins/rselements1/widgets/pricing-table/pricing-table--.php
