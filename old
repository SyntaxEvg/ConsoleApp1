# Ищем HTTP ссылки в базе данных
mysql -u uka_user -pdfvr4vre uka_site -e "SELECT COUNT(*) FROM wp_options WHERE option_value LIKE '%http://test.eaportal.nornik.ru%';" -s -N

# Если больше 0, нужно заменить
