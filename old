Да, вижу! В HTML коде есть класс wrap-v-3-1-calendar. Попробуйте так:
javascript// Найти элемент с этим классом
let calendar = document.querySelector('.wrap-v-3-1-calendar')
console.log(calendar)

// Посмотреть все свойства
console.dir(calendar)

// Попробовать найти Vue инстанс
console.log(calendar.__vue__)
console.log(calendar.__vueParentComponent)

// Или через дочерние элементы
let calendarInner = document.querySelector('.wrap-v-3-1-calendar .vc-pane-container')
console.log(calendarInner)
Также попробуйте:
javascript// Найти все элементы внутри календаря
let allElements = document.querySelectorAll('.wrap-v-3-1-calendar *')
console.log(allElements)

// Проверить, есть ли Vue на родительских элементах
let parent = document.querySelector('.wrap-v-3-1-calendar').parentElement
console.log(parent.__vue__)
Что покажет console.log(calendar) и console.dir(calendar)? Это поможет понять структуру и найти способ изменить даты.
