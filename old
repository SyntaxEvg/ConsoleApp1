    var content = $@"The password must:<br/>
        <ul>
        <li>include at least {Model.Settings.RequiredLength} characters{(Model.Settings.RequireUppercase ? ", one uppercase" : "")}{(Model.Settings.RequireLowercase ? ", one lowercase" : "")}{(Model.Settings.RequireDigit ? ", one digit" : "")}{(Model.Settings.RequireNonAlphanumeric ? ", one special character" : "")}</li>
        <li>not use any sequence of characters (e.g; 12345, qwert) nor contextual words (e.g.: hopex, mega)</li>
        <li>be complex enough to meet your enterprise security requirements</li>
        </ul>
        ";
            <p class="text-center">@T["Use the form below to change password for"] @Model.UserName.</p>
