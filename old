Основная архитектурная ошибка:
1. Для "Upd" не создается aApplicationInfo с aDeliveredCapabilities
vbscript
If sImpactTypeTemp <> "Upd" Then
    ' Создается полноценный aApplicationInfo с Delivered Capabilities
    Set aApplicationInfo = CreateObject("Scripting.Dictionary")
    tryAddDict aApplicationInfo, "aDeliveredCapabilities", aDeliveredCapabilities
    tryAddDict aApplicationsWithCapabiliries, sKey, aApplicationInfo
Else 
    ' Для "Upd" - только добавляется в общий список БЕЗ Capabilities
    If Not aApplications.Exists(sKey) Then  
        tryAddDict aApplications, sKey, aApplicationInfo ' ← aApplicationInfo НЕ СОЗДАН!
    End If
End If
2. В основном цикле - критическая ошибка:
vbscript
For each itemAllList in aApplications.Items
    ' itemAllList = aApplicationInfo, который НЕ БЫЛ СОЗДАН для "Upd"!
    If (aUnitInfo.Exists("sMilestone") And itemAllList.Exists("sMilestone") And 
        aUnitInfo("sMilestone")=itemAllList("sMilestone")) Then
        ProjectaCapabilities aCapabilities, itemAllList, aUnitInfo, ...
    End If
Next
Проблема: Для "Upd" aApplicationInfo не создается, но добавляется в aApplications, что приводит к ошибке при обращении к несуществующим свойствам.
Решение - исправить код:
Вариант 1: Унифицированная обработка
vbscript
Public Sub sType_Application(sKey,sImpactTypeTemp,ByRef aApplicationInfo, ByRef sImpactType,ByRef aApplications, ByRef aDeliveredCapabilities,ByRef aApplicationsWithCapabiliries,ByRef aCapabilitiesOfApplication, ByRef oDeliveredResource, ByRef sMilestone)
    
    ' ВСЕГДА создаем aApplicationInfo
    Set aApplicationInfo = CreateObject("Scripting.Dictionary")
    tryAddDict aApplicationInfo, "oItem", oDeliveredResource
    tryAddDict aApplicationInfo, "sType", sImpactType 
    tryAddDict aApplicationInfo, "sMilestone", sMilestone
    
    If aDeliveredCapabilities.Count > 0 Then
        tryAddDict aApplicationInfo, "aDeliveredCapabilities", aDeliveredCapabilities
    End If

    ' Для ВСЕХ типов добавляем в aApplicationsWithCapabiliries
    tryAddDict aApplicationsWithCapabiliries, sKey, aApplicationInfo
    
    ' Для "Upd" дополнительно добавляем в aApplications
    If sImpactTypeTemp = "Upd" Then
        If Not aApplications.Exists(sKey) Then  
            tryAddDict aApplications, sKey, aApplicationInfo
        End If
    End If
    
End Sub
Вариант 2: Минимальное исправление
vbscript
Else 
    ' Для "Upd" - создаем aApplicationInfo и добавляем в ОБЕ коллекции
    Set aApplicationInfo = CreateObject("Scripting.Dictionary")
    tryAddDict aApplicationInfo, "oItem", oDeliveredResource
    tryAddDict aApplicationInfo, "sType", sImpactType 
    tryAddDict aApplicationInfo, "sMilestone", sMilestone
    If aDeliveredCapabilities.Count > 0 Then
        tryAddDict aApplicationInfo, "aDeliveredCapabilities", aDeliveredCapabilities
    End If
    
    tryAddDict aApplicationsWithCapabiliries, sKey, aApplicationInfo
    If Not aApplications.Exists(sKey) Then  
        tryAddDict aApplications, sKey, aApplicationInfo
    End If
End If
Рекомендую Вариант 2 - минимальное изменение, которое исправит проблему для типа "Upd", сохранив существующую логику для других типов.
