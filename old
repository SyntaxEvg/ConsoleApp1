function sendCompressed(text) {
    // 1. Сжимаем Base36-LZ (если выгодно)
    // 2. Оборачиваем маркером
    if (text.length < 100) return text;
    var compressed = lzCompressBase36(text);
    return compressed.length < text.length ? '[C]' + compressed : text;
}
