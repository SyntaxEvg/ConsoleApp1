sudo nano /var/www/wp-content/plugins/header-footer-elementor/inc/widgets-manager/base/hfe-helper.php

public static function get_rollback_versions() {
    $rollback_versions = get_transient( self::TRANSIENT_KEY );

    if ( false === $rollback_versions ) {
        $max_versions = 30;

        require_once ABSPATH . 'wp-admin/includes/plugin-install.php';

        $plugin_information = plugins_api(   // <-- ЭТА СТРОКА 446 ВЫЗЫВАЕТ ОШИБКУ
Замените всю функцию на:
phppublic static function get_rollback_versions() {
    // Отключено для корпоративной среды без доступа к WordPress.org
    return [];
}
