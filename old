# Лог ошибок nginx
tail -50 /var/log/nginx/error.log

# Лог WordPress (если настроен)
cat /var/www/wp-content/debug.log 2>/dev/null | tail -50

# Проверим, включён ли WP_DEBUG
grep -i "WP_DEBUG" /var/www/wp-config.php

# Удаляем кэш
rm -rf /var/www/wp-content/uploads/elementor/css/*
rm -rf /var/www/wp-content/cache/*

# Восстанавливаем права
chown -R nginx:nginx /var/www/wp-content/uploads/elementor/

После этого зайди в админку WordPress → Elementor → Tools → Regenerate CSS.
